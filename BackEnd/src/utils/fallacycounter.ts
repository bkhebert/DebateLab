import Fallacy from "../database/models/Fallacy";

const increaseFallacy = (fallacyName) => {
  let fallacyNameNoSpace = fallacyName.replaceAll(' ', '')
  switch(fallacyName){
    case "Appeal to Authority": 
      
    break;
    case "Appeal to Ignorance":

    break;
  }
}